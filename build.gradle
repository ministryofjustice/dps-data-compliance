plugins {
    id("uk.gov.justice.hmpps.gradle-spring-boot") version "3.1.6"
    id "org.unbroken-dome.test-sets" version "3.0.1"
}

testSets {
    testIntegration
    testMessageIntegration
    testMessageIntegrationWithReviewPeriod
}

dependencies {

    // Lombok
    annotationProcessor 'org.projectlombok:lombok'
    compileOnly 'org.projectlombok:lombok'

    // Spring
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    implementation 'org.springframework.boot:spring-boot-starter-quartz'
    implementation 'org.springframework.boot:spring-boot-starter-security'
    implementation 'org.springframework.boot:spring-boot-starter-oauth2-resource-server'
    implementation 'org.springframework.boot:spring-boot-starter-oauth2-client'
    implementation 'org.springframework.boot:spring-boot-starter-reactor-netty'
    implementation 'org.springframework:spring-webflux:5.3.5'
    implementation 'org.springframework:spring-jms:5.3.5'

    // AWS
    implementation platform('software.amazon.awssdk:bom:2.16.39')
    implementation 'software.amazon.awssdk:rekognition'
    implementation 'software.amazon.awssdk:athena'
    implementation 'software.amazon.awssdk:sts'

    implementation platform('com.amazonaws:aws-java-sdk-bom:1.11.996')
    implementation 'com.amazonaws:aws-java-sdk-sns:1.11.996'
    implementation 'com.amazonaws:amazon-sqs-java-messaging-lib:1.0.8'

    // Utils
    implementation 'io.github.resilience4j:resilience4j-retry:1.7.0'
    implementation 'org.apache.commons:commons-lang3:3.12.0'
    implementation 'commons-io:commons-io:2.8.0'
    implementation ('org.quartz-scheduler:quartz:2.3.2') {
        exclude group: 'com.zaxxer'
    }

    //Jackson (spring versioned)
    implementation 'com.fasterxml.jackson.dataformat:jackson-dataformat-csv'

    runtimeOnly 'org.hsqldb:hsqldb:2.6.0'
    runtimeOnly 'org.flywaydb:flyway-core:7.7.3'
    runtimeOnly 'org.postgresql:postgresql:42.2.19'

    // Swagger
    implementation 'io.springfox:springfox-swagger2:3.0.0'
    implementation 'io.springfox:springfox-swagger-ui:3.0.0'

    // Test
    testAnnotationProcessor "org.projectlombok:lombok:1.18.20"
    testCompileOnly 'org.projectlombok:lombok:1.18.20'
    testImplementation 'net.javacrumbs.json-unit:json-unit-assertj:2.25.0'
    testImplementation 'com.squareup.okhttp3:okhttp:4.9.1'
    testImplementation 'com.squareup.okhttp3:mockwebserver:4.9.1'
    testImplementation 'io.jsonwebtoken:jjwt:0.9.1'
    testImplementation 'org.testcontainers:localstack:1.15.2'
    testImplementation 'org.awaitility:awaitility:4.0.3'
}
